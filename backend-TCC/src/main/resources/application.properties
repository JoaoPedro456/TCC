# ==========================================
# CONFIGURACAO PADRAO - Use profiles (dev/prod)
# ==========================================
# Ative com: --spring.profiles.active=dev  ou  --spring.profiles.active=prod
spring.profiles.active=dev

# JWT - valor padrao seguro para dev (sobrescreva via env em prod)
jwt.secret=${JWT_SECRET:bazani-mecanica-tcc-2026-secret-key-min-256-bits-required}
jwt.expiration=${JWT_EXPIRATION:39600000}
